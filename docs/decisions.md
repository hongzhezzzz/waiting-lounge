# Decisions log

Non-obvious choices we've made, one or two lines each. Append-only — if a decision is later reversed, add a new dated entry rather than editing the old one. That way you can see the history.

Format: `YYYY-MM-DD: <decision> — <one-line reason>`

---

- 2026-05-06: Use git locally with no GitHub remote yet — version control gives us rollback without needing an online account. We can add GitHub later if/when we need backup, sharing, or auto-deploy.
- 2026-05-06: Repo lives inside the user's Dropbox folder — convenient because it's already synced across the user's devices. Small risk: Dropbox can occasionally sync the `.git/` directory mid-write and corrupt history. If we ever see "fatal: bad object" or similar, that's the cause and we move the repo out of Dropbox.
- 2026-05-06: Default git branch is `main` — modern default, no other reason.
- 2026-05-06: Phase 1 hook writes to `~/.waiting-lounge.log` (the user's home folder), not into the repo — keeps event logs out of git history and out of Dropbox sync.
- 2026-05-06: Phase 2 frontend uses Next.js 14 App Router + Tailwind, scaffolded by `create-next-app` with TypeScript, ESLint, `src/` directory, `@/*` import alias — matches roadmap §5 and gives clean route folders for the screens listed in spec §7.
- 2026-05-06: Visual default is warm off-white (#FAF7F2) background, sage accent (#7FA98A), amber alert tone, generous whitespace, rounded-2xl cards, Geist Sans + small mono touches — aims at spec §14 "lightweight, friendly, slightly playful, low-pressure." Easy to swap once the user reacts.
- 2026-05-06: Demo Claude-needs-you alert wired as a small floating "▶ demo alert" button on `/chat` and `/board` — lets the user feel the alert without a backend. Removed/relabelled when Phase 5 wires it to real events.
- 2026-05-06: Mock chat replies happen in-component with a fake 1.2s peer reply — kept obvious as a demo (peer always says "(demo) — …") so spec §7 rule about "no fake data masquerading as real" stays honored.
- 2026-05-06: Phase 3 backend is TypeScript run via `tsx watch` rather than ts-node-dev — `tsx` is the simpler, well-maintained option in 2026 and needs no extra config. Same dev ergonomics (auto-restart on save).
- 2026-05-06: Backend port is 4000, frontend stays on 3000 — keeps the two dev servers separable and matches the placeholder `NEXT_PUBLIC_SOCKET_URL` default.
- 2026-05-06: Server **never logs message bodies** — only metadata (room id, sender handle, length). Spec §11 mandates "temporary message storage"; even though MVP storage is just in-memory, we set the precedent now so the production migration in Phase 6 doesn't accidentally start persisting bodies.
- 2026-05-06: `/chat` defaults to live (real socket); `?demo=1` keeps the fake-data path so the user can compare or show the app off without a backend.
- 2026-05-06: Anonymous handle generated server-side per connection (e.g. `gentle-diff-232`) — pulls from short colour/adjective + noun + 3-digit lists in `backend/src/lib/identity.ts`. No persistent identity in MVP.
- 2026-05-06: Block list is in-memory and per-socket — when a user blocks a handle, that handle is excluded from future matches *for the blocker only* during their current session. Persistent blocking lands in Phase 6 with the database.
- 2026-05-06: Known quirk on this machine — Next.js dev (HMR + file watcher) sometimes misses changes to files on `/mnt/c/...` (Windows filesystem under WSL2 + Dropbox sync), leaving the **server-side bundle stale** while the URL still returns 200. Symptom: page renders the previous version's content even after edits. Fix: stop the dev server, `rm -rf web/.next`, restart `npm run dev`. Hit this once during Phase 3 testing — the user saw fake chat data on `/chat?tag=...` because the SSR bundle for the chat route was cached from before the live-mode rewrite.
- 2026-05-06: "Find a new match" after peer-left was a `<Link>` to the same URL, which Next.js treats as a no-op. Replaced with a `searchNonce` state in `LiveChatWindow` — incrementing it forces the join effect to re-run and emit a fresh `join_queue`. Same trick is wired into the in-chat header "New match" link via an optional `onNewMatch` callback on `ReportBlockControls`.
- 2026-05-06: Phase 4 board lives at `/api/board` on the same Express server as the socket — one process to run, one CORS config. Endpoints: `GET /api/board?tag=`, `POST /api/board`, `POST /api/board/report`. In-memory `Map<id, BoardPost>`. 24-hour TTL, swept every 5 minutes. 10-second per-IP post cooldown (basic rate limit). Hide threshold: 3 reports auto-hide a post.
- 2026-05-06: Board posts get a freshly generated handle on every POST (no carryover from any chat handle) — there is no continuity of identity between a person's chat handle and their board posts. This is intentional for now: posting feels disposable, and we don't accidentally tie chat history to board history. Persistent identity is a Phase 6+ decision tied to durable storage.
- 2026-05-06: Frontend `MessageBoard` polls the API every 8 seconds — quick enough that posts from another window appear within seconds, slow enough that idle traffic is small. Replaced with a websocket push if board chat ever feels laggy.
- 2026-05-06: Board seeded with two welcome posts at backend startup so a fresh user doesn't land on an empty board (spec §9.2: "Do not leave the user on an empty spinner"). Seed posts are clearly meta ("Welcome. Posts vanish in 24h…") so they don't masquerade as real activity.
- 2026-05-06: **Phase 5 privacy paragraph** (per ground rule #6). The Claude Code hook payload path is now: Claude → `local-hook/hook.js` → POST `http://localhost:4000/api/agent-event`. Sanitization is double-enforced: (a) the hook script discards stdin entirely — never parses, never inspects, never logs the raw payload — and only sends a four-field object `{anonymousDeviceId, status, client, timestamp}`; (b) the backend route rejects any payload containing keys other than those four, so even if a future hook bug leaked something into the body, the server refuses it with `400 Unexpected field`. The hook swallows all errors (timeouts, connection failures, malformed responses) so it can never block or surface anything to Claude Code. Verified end-to-end: piping `{"prompt":"DO_NOT_LEAK","cwd":"/tmp/secret"}` to the hook produced only `agent_event {deviceIdShort, status, delivered}` in the backend log — zero leakage.
- 2026-05-06: Pairing model is **option C from roadmap §8.3** — `node local-hook/hook.js pair` prints a one-time URL to stdout. The user opens it once; the browser stores the deviceId in localStorage. Deliberately rejected option A (deviceId always in URL — easy to leak via referrer/screenshot) and option B (manual pairing code — extra step the user has to type). The pair subcommand also creates the `~/.waiting-lounge/device_id` file with mode 0600 if it doesn't exist.
- 2026-05-06: One global `ClaudeNeedsYouOverlay` mounted at the layout level (driven by the `AgentStatusProvider` context) — replaces the per-page overlays from Phase 2/3/4. Per-page "▶ demo Claude-needs-you alert" buttons now call `simulate("needs_attention")` on the provider instead of opening their own overlay, so the demo path and real path go through the exact same code.
- 2026-05-06: Header gets a `<LiveAgentStatusBadge />` that reads from the provider — shows "Not paired" until the user pairs the browser, then shows live status. Per-page badges (which were redundant after the global one) were removed from /chat, /board, /join.
- 2026-05-07: **Phase 8 deploy.** Frontend on Vercel (`https://waiting-lounge.vercel.app`, auto-deploys from `main`), backend on Render (`https://waiting-lounge.onrender.com`, free tier, auto-deploys from `main`), Postgres on Supabase. CORS (`ALLOWED_ORIGINS` env on Render) covers both `localhost:3000` and the Vercel URL. Local hook reads its backend URL from `~/.waiting-lounge/backend_url`.
- 2026-05-07: Frontend picks the backend URL at **runtime** based on `window.location.hostname` (localhost → `localhost:4000`, anything else → the Render URL) when no `NEXT_PUBLIC_BACKEND_URL` env var is set. Avoids needing a Vercel build-time env var for the friend pilot. If we later move the backend off Render, set the env var on Vercel instead of editing this fallback.
- 2026-05-07: Backend keeps a **last-known status per device** in memory and replays it to a browser as soon as it `register_device`s. Without this the badge stayed "Not connected" until the next hook event, defeating the intended UX (Claude starts → user opens lounge → badge already shows "working"). Stored in-memory only — when Render free-tier sleeps the backend (~15min idle) the map is wiped; the badge will show "Not connected" until the next hook event arrives. Promoting this to Postgres is a candidate fix if friends complain.
- 2026-05-07: **`PostToolUse` hook → `waiting`** wired up so the badge flips back to "Claude is working" after the user approves a permission and the tool actually runs. Without it, the badge stayed amber for the rest of the assistant turn after approval. Implemented as a new `resume` subcommand in `local-hook/hook.js` plus a `PostToolUse` entry in `.claude/settings.json`.
- 2026-05-07: **Known badge edge cases left unfixed** (deferred — none are blocking the friend pilot):
  1. *Interrupt/deny via the interrupt button leaves badge stuck at "Claude needs your attention".* When the user hits the interrupt button while a permission dialog is up, neither `Stop` nor `PostToolUse` fires, so nothing clears the amber state until the next `UserPromptSubmit`. Fix candidate: a frontend TTL (after ~90s, treat `needs_attention` as `done`).
  2. *Closed Claude Code freezes the badge at last status forever.* No `SessionEnd` → `done` mapping today. Same TTL as #1 would cover it.
  3. *`Notification` fires for non-permission idle nudges too.* Those repaint the badge amber even when Claude isn't actually waiting on the user. Lives with #1 — TTL plus, eventually, distinguishing notification subtypes if Claude Code's hook payload exposes them.
  4. *Render free-tier sleep wipes the in-memory last-status map.* Long idles → "Not connected" until next hook event. Fix: persist last-status to Postgres.
  5. *Multi-folder collision.* Two Claude Code sessions on the same machine share one device id; their events interleave on the same badge (newest wins). Fine for now since identity is per-machine, not per-folder; revisit if friends actually run two concurrent agents.
- 2026-05-07: **Phase 11 — accounts and points.** First time the lounge has any persistent identity. Sign-in is **6-digit OTP email via Supabase Auth** (chosen over magic-link to avoid the orphaned-tab UX trap, and over GitHub-only so non-GitHub friends can sign in). Anonymous handles still appear in-game — auth is for *persistence of points*, not de-anonymizing. The local hook is **untouched** and still only sends the 4-field sanitized payload.
- 2026-05-07: **Phase 11 game design — Spot the Bug.** Two players see the same 8-line buggy snippet; first to click the offending line wins the round. Picked over Type Race (swap recommended by Plan agent — keyboard speed is a worse fit for a dev audience than real bug-finding skill, same backend cost). Bank starts at ~30 hand-curated snippets across JavaScript / Python / SQL covering off-by-one, async ordering, JS coercion, mutation-during-iteration, and other classic gotchas (`backend/src/games/spotTheBug/snippets.json`). Round count is 3/6/10 for 1/5/10-min games; round timeout 45 s; post-round pause 2.5 s.
- 2026-05-07: **Phase 11 staking model.** Fixed 100-point ante v1, both players ante at game start, winner takes 200, tie returns stakes. Starting balance 1000 pts on first sign-in. Picked fixed ante over a chooser to keep the friend-pilot UX trivial; player-chosen ante can be added in Phase 13+.
- 2026-05-07: **Phase 11 atomic points.** All point mutations go through `backend/src/games/transferPoints.ts` inside a single Postgres transaction with `SELECT … FOR UPDATE` ordered by user id (prevents deadlock between concurrent games). At ante time we also write a `pending_refunds` row per player; settle marks them `processed_at = now()`. If the backend dies mid-game, on next startup `processStalePendingRefunds()` credits the antes back and marks the `game_rounds` row `outcome = 'aborted'`. Smoke-tested both happy-path settle and cold-start refund against the live Supabase DB; results: ante 100 → balance 900, win → 1100, tie → 1000, abort → 1000.
- 2026-05-07: **Phase 11 socket auth is optional, not required.** The middleware verifies a Supabase JWT if present; if absent or invalid the connection is anonymous (still works for chat/board/lounge). Game events (`queue_for_game`, `game_action`) check `socket.data.userId` and reject if missing. Keeps the existing flows working for users who haven't signed up yet.
- 2026-05-07: **Phase 11 cross-machine room reuse.** Game peers join the same Socket.IO `roomId` model that chat already uses (instead of building a parallel "game room" concept). Means chat-while-gaming is essentially free in Phase 14. Game framework is registry-driven (`backend/src/games/index.ts` registers each game type's runner constructor) so Phase 12's Brain Bet only needs a new resolver class.
- 2026-05-07: **Phase 11 Node 18 polyfill.** `jose` v6 went webapi-only and expects `globalThis.crypto`, which Node 18 doesn't expose. Polyfilled at the top of `backend/src/server.ts` from `node:crypto.webcrypto`. Harmless on Node 20+ where `globalThis.crypto` is already set; can be removed once the user's local env is on 20.
- 2026-05-07: **Phase 11 anti-abuse coverage.** Self-match (same user_id on both sides of `queue_for_game`) is rejected by the queue's eligibility filter. Device-account binding is keyed on `device_id` and 409s on a second account trying to claim the same device. Disconnect-during-game gives a 10 s grace timer before forfeit (handled by the resolver — same-user reconnect cancels the timer). Cold-start refund covers any game that died before settle. Skipped for v1: per-IP socket-level rate limit on game starts (5 friends, low blast radius).
- 2026-05-07: **Phase 14a — refill, leaderboard, game history.** Daily +100 refill is **lazy**, only triggered from `GET /api/me` (not from the socket auth middleware). Reason: the toast lives on the `/me` page, and if the socket consumed the refill silently the user would never see the toast. Refill is wrapped in a Postgres transaction with `SELECT ... FOR UPDATE` on the user row — re-checks eligibility *inside* the lock to be safe under concurrent calls. Leaderboard is public (no auth), capped at 100 entries, ordered by points desc. Game history endpoint joins `game_rounds` to `users` twice (once per player) so the response includes the opponent's handle. **For v1 the history doesn't include final scores** — only outcome (win/tie/aborted) and `winner_id` exist on `game_rounds`. Adding score columns is a small ALTER for a future phase. New engineering-lessons doc tracks classes of bugs across phases — the Phase 14a build added the `react/no-unescaped-entities` lesson (apostrophes in JSX text fail Vercel's prod lint).
- 2026-05-07: **Phase 14b — chat-while-gaming, frontend-only.** The existing `chat_message` socket handler in `sockets.ts` is room-agnostic (`socket.to(me.roomId).emit(...)`) — game peers already share a `roomId`, so chat in-game routed correctly the moment we added a UI. New `<ChatPanel>` is a layout-bottom-right collapsible component, mounted in both game-shell branches. Optimistic local rendering (server's `socket.to()` doesn't echo back to sender). Open/closed state in localStorage. No backend touch.
- 2026-05-07: **Phase 14c — Stock Direction round.** Synthetic chart data from a seeded LCG random walk (committed `stockDirectionBank.json`, 40 windows × 60 prices, six personalities, balanced 20-up/20-down). Avoids any external API dependency or live data. Direction is the primary scoring axis; magnitude is the tiebreaker. ASCII sparkline UI with Unicode block chars — fits the lounge's typographic vibe and avoids SVG/Chart.js bloat. Round timeout 30s.
- 2026-05-07: **Phase 14d — invite-from-lounge.** New `Invite` map in `state.ts`, four socket events (`list_idle_users`, `invite_to_game`, `accept_invite`, `decline_invite`), 30s expiry timer. Idle = `userId != null && roomId == null`; deduped by userId so multiple tabs of the same account don't show twice. Did NOT filter game-queued users — accepting pulls them out of any queue. Refactored `queue_for_game`'s match logic into a shared `startGameBetween(io, a, b, gameType, dur, ante)` helper so the invite path doesn't duplicate ante-charging or runner-creation. Layout-level `<IncomingInviteBanner>` listens for `incoming_invite` on any signed-in page; on accept, also listens briefly for the resulting `game_started` to redirect both peers to the game page. Lounge polls `list_idle_users` every 10 s — push-based broadcasts are over-engineering for ≤5 friends; revisit if friends complain about staleness.
- 2026-05-07: **Stage 3 kickoff — multi-agent design pass.** User asked for a team: market research + UX/design + game-design + engineer-architect. Three research agents ran in parallel, persisted findings to `docs/research/{market-survey,ux-principles,game-mechanics}.md`; engineer-architect synthesized into `docs/research/engineering-plan.md`. Convergent themes: (a) "looks plain" is really "feels dead" — fix is live state, not less copy; (b) Lichess + Codingame Clash of Code are our two closest analogs (both flagged independently by market and UX); (c) Brain Bet 2.0 is a phased state machine with 1000-chip running stack, three of five planned features map cleanly to existing 7 round types; (d) Daily Brain Bet is the cheapest retention win (Wordle pattern). Five engineer-recommendation calls made on user's behalf per "make the reasonable call and continue": (1) pool matchmaking ships in 3a, bot fill in 3b separately; (2) replace old Brain Bet outright (don't keep "Classic"); (3) Daily is solo Wordle-style (not multiplayer); (4) terminal play deferred to Stage 4 candidate (matching wait is solved by 3a/3b pool+bot, TUI is delight not bottleneck); (5) bot fill ships as one calibrated bot, not three difficulties. User can redirect any of these.
- 2026-05-07: **Stage 3 plan structure.** Three sub-stages with ship-and-poke gates. 3a = 5 quick wins, 1-3 days each (nav/leaderboard, homepage live cards, lounge ticker, Lucide icons, pool matchmaking). 3b = 3 medium efforts, 3-7 days each (Brain Bet 2.0, Daily, bot fill). 3c = Stage 4 candidates (terminal play, solo Storm). Phase ordering dependencies: 3b.1 blocks 3b.2 (Daily reuses new betting loop); 3a.5 blocks 3b.3 (bot fill needs a pool to fill into); 3c.1 and 3c.2 both depend on 3b.1 (TUI and Storm both render the new betting UI).
- 2026-05-07: **Brain Bet 2.0 critical engineering invariants** (per `docs/research/game-mechanics.md`). (1) Bets MUST close before answers are revealed — strict per-round phase machine `reveal → bet → answer → showdown`. The current `handleAction()` accepts answer events at any time and must be replaced with phase guards. If a client can peek at the answer and *then* bet, the game collapses to "always bet when correct" and there's no game theory left. (2) Every round type must support a Fold action — a low-stack player must never be auto-bankrupted; they must be able to surrender only the 50-chip forced ante, not their whole stack. Trivia rounds get a fold too ("I know the answer but want to save chips" is a valid play). (3) Running-stack chips are NOT user account points — they live in `runner.state` only. `transferPoints.ts` (`chargeAntes`, `settleGame`) does not change. The runner picks `winnerSocketId` from chip lead at end-of-match and passes it to `settleGame()` as today. Crash recovery / refund / cold-start path keep working with zero modification because the chip stack never touches Postgres.
- 2026-05-07: **Stage 3a.1 — header nav.** Added Leaderboard link, renamed Join→Lounge (verb→noun, matches Lichess convention; primary "place to be" is the lounge after Stage 2). Order: Lounge · Board · Leaderboard · About. The `/join` route still works as a deep-link from the homepage CTA / board / chat back-link / pair-success page — only the header label changed; nothing was deleted. Privacy invariant unaffected.
- 2026-05-08: **Stage 3 fix — game-start race.** First fix (PR #17) added an 800 ms delay between `game_started` and `round_start` to give clients time to mount the game page. Insufficient — Next.js routing + React subscribe takes longer than 800 ms in practice (user reported game still slow to start). Real fix (PR #18): client emits `request_round_state { gameId }` on game-page mount, runner replays current state to that socket. Idempotent. No more artificial delay. Also covers tab-refresh during a game and network-blip recovery, since the same "ask on mount" path catches up state regardless of cause. Extracted per-round-type emit logic in `brainBet/resolver.ts` into `emitRoundStartTo(round, socketId)` so startRound (loop over players) and replayCurrentState share one code path.
- 2026-05-08: **Stage 3 fix — needs-attention sticky overlay.** "Return to terminal" used to only close the modal, leaving `meta.status` at `needs_attention`. Notification hooks fire in clusters during one Claude turn, so the modal would re-pop on every event. Fix: `acknowledge()` flips status to `done` AND suppresses inbound `needs_attention` updates for 5 s. Other status flips (`waiting` / `done`) still pass through immediately. Snooze button does NOT acknowledge — re-opens after 30 s as before.
- 2026-05-08: **DEFERRED — calibrate forfeit penalty.** (Recorded as Task #44, deferred until 3b.1 lands.) Today's behavior: voluntary leave during a game calls `runner.abort()` which refunds both antes via `pending_refunds`. Unfair — a losing player can rage-quit and walk out even. Future fix should scale the forfeit by game progress (rounds played / time elapsed) and current chip lead (in Brain Bet 2.0). Brain Bet 2.0's 1000-chip running stack already encodes this naturally (quitter loses their stack, opponent takes the pot). Spot the Bug needs an explicit progress metric. Cold-start refunds (server-died) and disconnect-grace forfeits remain at the existing 100% / 0% extremes — only voluntary mid-game leave should be calibrated.
- 2026-05-08: **Stage 3a complete.** All five 3a items shipped to prod via PRs #12 / #15 / #16 / #19 / #20. Pacing: each was small enough that the user merged a stack of them within a single session. Visual polish that explicitly chose Lichess + Codingame Clash of Code as references (per market-survey + ux-principles convergence). Stops using emoji throughout — Unicode glyphs render inconsistently across OSes and break the cream/sage palette. Lucide is the chosen icon set (ISC, tree-shakeable per icon, ~1 KB each). Pool matchmaking single shared key per gameType — pool users coexist with explicit-tuple queueers on the same `gameQueueKey`.
- 2026-05-08: **Stage 3b.1 — Brain Bet 2.0.** Phase machine `reveal → bet → answer → showdown`. 1000-chip running table stack (UI fiction — platform-points settlement still moves the 100-pt ante / 200-pt pot exactly as before). Forced 50-chip ante per round + voluntary raise (+25/+50/+100/all-in) or fold. Bet tiers committed simultaneously, locked privately until the 8s window closes. Pot allocation: both fold → house keeps; one fold → opponent takes pot; neither folds → round winner takes pot (or split on tie). Match ends on bust > rounds-complete > wall-clock. Winner = chip leader. Three critical invariants: (1) bet phase guard rejects answer actions outside `phase==="answer"` — most important; (2) every round supports fold; (3) chip stacks live in `runner.state` only, never touch Postgres. Frontend: shared `BrainBetRoundView` extension with `phase`, `pot`, `chipStacks`, `myBet`, `peerBet`, `onBet`. Round body is dimmed during reveal/bet so the user's eye lands on the bet panel. Fold-resolved banner renders for fold cases since per-round-type reveals don't cover them.
- 2026-05-08: **Stage 3b.2 — Daily Brain Bet.** Single curated 3-round puzzle per UTC day (1 estimation, 1 big-O, 1 geo trivia), deterministic seed by date string (FNV-1a hash + Numerical-Recipes LCG). Estimation tolerance: within 25% of the truth (looser than multiplayer "closer wins" since no opponent bounds the guess). Streak grows by 1 if last_play === yesterday; resets to 1 otherwise. Idempotent /submit returns existing score on duplicate (does NOT re-score or overwrite). Separate /status endpoint for non-destructive "did I play today?" lookup — replaces an earlier draft that called /submit with empty answers (would have written a 0-score). New `withTx()` helper in `db/index.ts` for transactional inserts. New `<StreakChip>` in header (flame icon, only visible when streak > 0). Header chip masks current to 0 if last_play is more than 1 day old — UI honesty over a streak that's already broken server-side.
- 2026-05-08: **Stage 3b.3 — Bot fill.** When `queue_for_pool` waits 30 s without a real peer, `startBotMatchFor` builds a Game with `isBotMatch=true` and a synthetic `botSocketId="bot:<uuid>"`. Bot's handle is `lounge-bot-NNN` — that prefix is the public signal the frontend uses to render the "no points" amber banner. Bot strategy is "honest amateur": 70% check / 15% +25 / 10% +50 / 5% fold for bets; ~55% correct on objective rounds; info-aware on Indian Poker (sees the human's card, folds on high cards); always-switch on Monty Hall. `endGame` and `abort` skip `settleGame` when `isBotMatch` — no DB writes, no leaderboard impact. Bot driver lives in the Brain Bet runner (not a separate process); on `openBetPhase` and `openAnswerPhase` it schedules the bot's action on a 1.5–6 s randomized delay. CLAUDE.md ground rule #5 ("no fake data masquerading as real") respected via the `lounge-bot-` prefix and the upfront amber banner.
- 2026-05-08: **Stage 4 kickoff — terminal play (`waiting-lounge play`).** Decision matrix at start: (a) **TUI library = ink** (React-for-CLI), over blessed/neo-blessed (imperative, Windows-flaky) or raw readline+ANSI (layout math by hand). Codebase is already React; ink lets renderer share mental model and yields readable diffs. ~1.5 MB install — one-time `npm install` cost. (b) **Round-type coverage = all 7 from day one**, over splitting matchmaking pools by client kind. Reason: pool fragmentation kills tiny userbase; landing on an unsupported round mid-match is brutal UX. (c) **CLI matchmaking = pool only**. Bot fill from 3b.3 guarantees pair within 30 s; invites would need a polled lounge member list (scope creep). (d) **Forfeit = confirm-then-disconnect**, reuses existing 10 s server grace + abort path. No new server logic. (e) **Same-pool with browser** — TUI vs browser match works because both use identical socket events. Five ship-and-poke phases: 4a skeleton+connect, 4b auth bridge, 4c match flow + Indian Poker, 4d remaining 6 round types, 4e polish.
- 2026-05-08: **Stage 4a — TUI skeleton.** `cli/play.mjs` (ESM ink) added as a new dispatcher case in `cli/waiting-lounge.js` (CJS) via dynamic `import("./play.mjs")`. Lifted shared config helpers from `waiting-lounge.js` into `cli/lib/config.js` so both processes use the same on-disk reads (`~/.waiting-lounge/{device_id,backend_url,frontend_url}`). JSX intentionally avoided — no build step, so the file uses `createElement` aliased as `h`. Anonymous socket only at this phase; auth lands in 4b. ~150 LOC.
- 2026-05-08: **Stage 4b — auth bridge** (CLI ↔ browser code-exchange). 32-byte hex code with 5-min TTL stored in-memory on backend (`backend/src/routes/cliAuth.ts`). Three endpoints: `POST /start` (CLI registers code), `POST /finalize` (browser, auth-required, sends accessToken + refreshToken + supabaseUrl + anonKey), `GET /poll` (CLI polls every 2 s, single-use — deletes entry on retrieval). Code-tail visual confirmation in browser to defend against socially-engineered code-takeover. Supabase URL + anon key stored alongside tokens at `~/.waiting-lounge/auth_token` (mode 0600) for direct REST refresh against `/auth/v1/token?grant_type=refresh_token` — keeps the user authenticated for ~30 days without browser. Privacy paragraph reaffirmed: TUI never sends Claude Code content, hook payload still 4 fields, hook never reads `auth_token`.
- 2026-05-08: **Stage 4c — match flow + Indian Poker.** `useReducer` state machine with appPhase {auth, pairing, connecting, lobby, searching, in_match, match_end, error}. Lobby F → emits `queue_for_pool`; bot fill from 3b.3 carries the rest. All Brain Bet 2.0 socket events wired (round_start, phase_change, bet_recorded, bet_phase_closed, decision_recorded, round_resolved, game_resolved, game_aborted). `request_round_state` emit on `game_started` for mount-race safety (mirrors browser pattern). Indian Poker fully rendered (two card boxes + B/F); other 6 round types render `PlaceholderRound` showing the prompt only — user can bet but can't answer (4d adds full input).
- 2026-05-08: **Stage 4d — remaining 6 round types.** Per-type input maps: estimation/monty_mirage = digits + . + Enter (numeric buffer); chicken = 1–9 + 0 (=10); big_o/geo_trivia = 1–N for choices; stock_direction = U/D first → digits + Enter (Backspace at empty buffer returns to direction picker). Optimistic local lock on emit — server's `*_recorded` events are no-op confirmations. Big-O code rendered plain (no syntax highlighting; cli-highlight would add ~80 KB, deferred unless the user finds the code hard to parse). Stock Direction sparkline = pure `(numbers) → string` mapping each value to one of `▁▂▃▄▅▆▇█` via min/max normalization (`cli/lib/sparkline.mjs`); reveal renders the full 60-bar series of the next 30 minutes' actual prices.
- 2026-05-08: **Stage 4e — polish.** (1) Forfeit confirm dialog: Q in in_match no longer exits directly — shows a red "Forfeit match? Y/N" overlay; Y disconnects (server's 10 s grace gives opponent the win); N cancels and restores match. (2) Reconnect overlay: socket disconnect mid-match shows a yellow "⟳ Reconnecting…" banner instead of a generic toast; on reconnect, automatically emits `request_round_state` to resync (also handles `game_reattached` server event). (3) `MatchEndScreen` distinguishes `game_aborted` (yellow border, "Antes refunded") from `game_resolved` (green/red/yellow by outcome, payout line). (4) `/cli-pair` page shows the code tail in a large amber-bordered box with `text-3xl tracking-widest`, replacing the small inline chip — the user reported the previous version "flickered past" when they were already signed in.
- 2026-05-08: **Stage 5.1 — chat-while-playing.** TUI: input multiplexing in `useInput` — chat mode short-circuits all keys to chat input; T toggles in (when in_match), ESC toggles out, Enter sends. New ChatPanel component with constant 7-line height (5 padded message slots + always-present input row) so adding a message or toggling chat mode does NOT change the frame's total line count — that fixes the terminal "shaking" bug ink was producing on send. Bodies truncated to 60 chars to prevent wrapping. Web: ChatPanel refactored from floating bottom-right popup to inline collapsible card below the game; default open=true with a localStorage-persisted state, header acts as collapse toggle, unread badge when collapsed.
- 2026-05-08: **Stage 6 — dock the lounge alongside Claude Code (planning).** Direct embedding inside Claude Code's chat is not possible — Anthropic exposes no panel/footer/sidebar surface for third parties (per official docs). Closest available: same terminal window, two adjacent panes, separate processes. Two paths: (a) tmux/zellij wrapper, ~3 days; (b) custom Node+node-pty PTY multiplexer, ~3 weeks. Decision: build BOTH, in order. Stage 6a (tmux) is the developer-internal beta + foundation; ~80 % of its artifacts (CollapsedStrip, --dock flag, state.json writer, height-conditional render) carry into 6c without rework. Stage 6c (custom multiplexer) is the production ship target — locked by Ground Rule #11 (frictionless-first, ≥20 % threshold). Layout = binary toggle on Ctrl-L between ~1 row collapsed and ~30 % expanded; multi-size cycle deferred until/unless user misses the chat-only middle state.
- 2026-05-08: **Ground Rule #11 — Frictionless-first.** App-wide design principle: when choosing between an option that requires extra install/setup/config and one that doesn't, default to the zero-setup option unless the dependency is overwhelmingly common in the target audience. Quantitative bar: ≥20 % of target users would need an extra setup step → build the version that doesn't require it. Locked in CLAUDE.md as Ground Rule #11; applies app-wide, not just to dock decisions.
- 2026-05-08: **Stage 6a — tmux dock + folded-in fixes.** Single PR, 7 commits, ~3.5 days. Folded into the dock build per user direction ("fix while building"): (1) auto-scroll regression fix in `play` (top-level Box now has `height: process.stdout.rows` + `overflow: "hidden"` so ink clips instead of letting the layout grow past visible area); (2) bot-now feature — new `start_bot_match_now` socket event reuses `startBotMatchFor`; TUI lobby gets [B] key, web lobby gets "Play a bot now" button. Dock-specific commits: `--dock` and `--write-state-to=<path>` flags + atomic state.json writer; CollapsedStrip (1-row indicator, height-conditional render via `WL_DOCK_COLLAPSED_THRESHOLD`); `cli/dock.js` tmux orchestrator with Ctrl-L toggle (root-table bind so it fires from either pane). Layout config-driven (`WL_DOCK_COLLAPSED_ROWS`, `WL_DOCK_EXPANDED_PCT`, `WL_DOCK_TOGGLE_KEY`).
- 2026-05-08: **Process — Claude performs merges with directional confirmation.** User explicitly delegated `gh pr merge` and similar fiddly git/gh actions to Claude. Claude pre-verifies origin matches local before merging, post-verifies merged commit contains expected changes (per Ground Rule #10), and runs the merge directly when the user has signaled "go" — no more writing out commands for the user to copy-paste.
- 2026-05-08: **Stage 6b — Claude Code statusline integration.** `cli/statusline.js` reads `~/.waiting-lounge/state.json` (written by `play.mjs` Stage 6a) and prints a one-line summary on each statusline tick. Decision: separate command (`waiting-lounge statusline`), not auto-merged into `~/.claude/settings.json` during install — auto-merging risks clobbering the user's existing statusline composition. Docs guide paste-in setup in `docs/statusline-setup.md`. Stale-after threshold = 30 s so the bar degrades gracefully to `☕ Lounge: idle` when the dock isn't running. ~50 LOC; no network I/O; reads only the local state file.
- 2026-05-08: **Stage 6c.1 — zero-dep PTY multiplexer.** `cli/multiplexer.js` (CJS) spawns two child PTYs via node-pty: `claude` in the top region (sized to `cols × topHeight`, no output filtering needed because claude only knows about its own size), and `node cli/play.mjs --dock` in the bottom region (sized to `cols × bottomHeight`, output filtered with regex CUP/HVP/VPA row-shifts to map lounge's row N → terminal's row topHeight+N). DECSTBM scroll region pinned to top; lounge can't override (escape sequences for scroll-region/alt-screen/clear-screen are stripped from lounge's stream). Stdin multiplexed: Ctrl-L (byte 0x0c) toggles focus + bottom-region size between collapsed (~1 row) and expanded (~30 % of terminal). Default behavior preserves tmux when available — multiplexer kicks in only with `--no-tmux` flag, `WL_DOCK_NO_TMUX=1` env, or when tmux is missing. Multiplexer module lazy-loaded so tmux users don't pay node-pty's native-import cost. Output translator has 12 unit tests (`cli/__tests__/multiplexer.test.js`, node:test) covering CUP/HVP/VPA, Home, multi-escape chunks, color, and the three defensive blocks. Honest scope: this is a first cut — alt-screen handling, resize edge cases, and per-terminal-emulator validation deferred to 6c.2. Per Ground Rule #11 (frictionless-first), this gets us to "no tmux required" — the production ship target.
- 2026-05-08: **Stage 6c.1 first-test investigation.** User tested `node cli/waiting-lounge.js dock --no-tmux` and reported "the lounge takes input when not expanded; only first keystroke registered." Investigation: routing logic was verified correct via two tests — (a) a unit test mocking `pty.spawn` showed all 5 routing scenarios pass (collapsed → claude, Ctrl-L toggle, expanded → lounge, toggle back, multi-byte arrow keys); (b) a scripted-stdin test against the real multiplexer captured claude's output and showed `❯ hello` rendered correctly at claude's prompt with all 5 chars present. **Conclusion: the user's report was display confusion, not a routing bug.** Two visual artifacts were the cause: (1) claude's input echo lands at claude's cursor position, often at the bottom of its region — visually adjacent to the lounge strip, easy to misattribute; (2) no focus indicator, so the user couldn't tell which pane was active before they typed. Fix in this commit: a focus-indicator banner that flashes on toggle and at startup ("▶ Focus: claude — press Ctrl-L to switch to the lounge and play"); a clearer affordance in `CollapsedStrip` ("^L to enter & play" instead of just "^L expand").
- 2026-05-08: **Stage 7 — Distribution via public GitHub.** Per Ground Rule #11 (frictionless-first), Path A wins: public repo + `npm install -g github:hongzhezzzz/waiting-lounge`. Pre-public secrets audit identified only one blocker (dev-machine path in `local-hook/README.md` and tracked `.claude/settings.json`); fixed by replacing paths with `/path/to/waiting-lounge` and untracking the per-user settings file. Public-facing root README rewritten with tagline + ASCII dock mockup + install one-liner + privacy section quoting the actual 4-field hook payload. `package.json` got description, homepage, repository, bugs, keywords, LICENSE in `files[]`. Verified end-to-end before flipping public via tarball install in isolated `/tmp/wl-prefix`; verified post-public via auth-stripped `GIT_TERMINAL_PROMPT=0 npm install -g github:…` from a clean directory (per Ground Rule #12, customer-facing tests).
- 2026-05-08: **Stage 8 — TUI design system.** Built `cli/lib/theme.mjs` as the single source of truth: semantic color tokens (`C.brand`, `C.success`, `C.warning`, `C.danger`, `C.peer`), border tokens (`B.primary` round, `B.panel` single, `B.strong` double), brand identity, round metadata, plus shared components (`Banner`, `Footer`, `Hint`, `Key`, `Title`, `PhasePill`). Six explicit design rules at the top of the file: semantic-not-literal color, hierarchy through weight not shouting, `[K]` everywhere for keys, every scene gets a one-line dimmed footer listing active keys, Unicode `…` always, imperative + lowercase verbs after [K]. Every component then refactored to import from the theme: ChipBar gets formatted chip counts (1,180); BetPhasePanel timer turns red+bold when ≤3s; MatchEndScreen never blanks (shows "Finalizing match…" placeholder when `end` is null); RevealCard uses outcome-coded borders + win/lose icons; ChatPanel preserves constant-height design but with colored handle prefixes; round renderers share new `PhaseHint`/`LockedLine` helpers (lifted from 6 round files, no duplication); CollapsedStrip uses `[^L]` bracketed key with context-aware `enter`/`open` verb. CLI command output (`help`, `install`, `status`, `attach`, multiplexer focus banner) all polished to the same design language. No new behaviors, no protocol changes — pure render-layer polish. Goal per user direction ("near fully polished window"): match the visual quality bar of charm.sh-style TUIs without adopting their full toolkit.
- 2026-05-08: **Process — test interactive code yourself before asking the user.** When Claude ships interactive UI behavior (multiplexer, TUI, etc.), Claude should attempt to test it before claiming it works. Two patterns that work without taking over Claude's terminal: (a) unit tests with mocked dependencies (e.g., stub `pty.spawn` to return controllable fakes; verify routing); (b) scripted-stdin tests using `( printf "input"; sleep 1 ) | timeout 5 node …` to feed bytes into the real program and capture output. User's remark: "can you not do this kind of test on your end? I would imagine you could."
- 2026-05-08: **Stage 6d — attach to an existing claude session via tmux.** User asked: "can a user open the lounge using an existing claude chat, but only when open a new one? Any way we can achieve 'opening anytime' feature?" Answer: yes, but only via tmux — and only if claude is already running inside tmux. The tmux server-client model lets any other tmux client `split-window` on an existing session, so we can add a lounge pane mid-claude. The kernel sends claude a SIGWINCH; claude redraws smaller; the new pane runs the lounge. Cannot be achieved via the zero-dep multiplexer because the multiplexer has to wrap claude at startup (it owns claude's PTY) — once claude is running outside it, there's no clean way to retroactively wrap. New command `waiting-lounge attach` (cli/attach.js, ~70 LOC). Frictionless-first guideline (Ground Rule #11) applies: the path of least friction for the user is "install tmux once, run `tmux` before `claude`, then `attach` works any time." For users without tmux mid-session, the recommendation is `waiting-lounge dock` for the next session.
- 2026-05-08: **Ground Rule #12 — Customer-facing test principle.** User said: "Clean the friction obstacle, and I will test it afterwards. You should prioritize each test with me as a 'customer facing' test, unless you are stuck and needs input." Triggered by the `! waiting-lounge attach` test failing with `command not found` because the binary wasn't on PATH (npm link hadn't been run). Locked into CLAUDE.md as Ground Rule #12: when asking the user to test, pre-walk their environment; if any documented step would fail (PATH, perms, env vars, deps), fix it FIRST. Reserve user tests for things genuinely needing their input (visual feel, real claude, real human). The dev-setup `npm link` note now lives in docs/status.md to prevent the same friction for future clones.
- 2026-05-13: **Stage 9 — Frictionless one-line install.** User: "Can we automize 'paste the printed JSON into ~/.claude/settings.json'? I expect a one line installation on the user's side, and functions start working." Roadmap §9 originally said "prefer not to silently modify Claude Code settings"; we're overriding that here for three reasons: (a) the privacy invariant is unchanged — the hook still only ever sends 4 sanitized fields; (b) the user explicitly asked for one-line install with no manual paste, which Ground Rule #11 (frictionless-first) reinforces; (c) we write a timestamped backup at `~/.claude/settings.json.bak.<ISO>` before any edit, so "go back to last working version" is one `cp` away. The merge logic preserves unrelated top-level keys and any existing hook entries from other tools; second runs are idempotent (our entries are replaced cleanly, not duplicated). Pair URL also auto-opens via platform-aware shell (`open`/`cmd.exe /c start`/`xdg-open` or `$BROWSER`) with the URL printed as fallback. WSL is detected via `WSL_DISTRO_NAME`/`WSLENV`/`WSL_INTEROP` and routed through `cmd.exe` because xdg-open is broken in most WSL setups. Opt-outs preserved: `--print-only` (zero settings/browser touch), `--no-open` (skip browser only), `--ask` (legacy prompt — defaults to Y). The customer-facing one-liner now: `npm install -g github:hongzhezzzz/waiting-lounge && waiting-lounge install` → one click in the browser → `waiting-lounge dock`.
- 2026-05-13: **Stage 9b — Headless detection on auto-open.** Added `isHeadless()` to gate the browser-open call. Triggered by user question on cross-platform polish: SSH / Docker / CI users on a Linux box without `$DISPLAY` were going to see a confusing `xdg-open: no method available` stderr line after a successful install. The detection rules: macOS / native Windows / WSL / `$BROWSER`-set → never headless; native Linux without `$DISPLAY` and without `$WAYLAND_DISPLAY` → headless. SSH-with-X-forwarding sets `$DISPLAY` automatically, so it falls through to the xdg-open path correctly. On headless boxes, we print a clear message ("No graphical display detected — copy the link above into a browser on your local machine") instead of attempting the open. URL is always printed regardless, so functionally nothing changes — this is pure polish.
- 2026-05-13: **Stage 9c — Cold-start UX + Windows experimental labeling.** Tier-1 polish triggered by user's priority framework (install/access > frictionless > viscosity). Render free-tier sleeps after ~15 min idle; the first request after sleep takes 30–50 s. Before this fix, a fresh user's `waiting-lounge test` could timeout at 5 s and surface as "Couldn't reach the backend" — indistinguishable from a real install failure. Fix shape: fast attempt first (4–5 s preserves the warm-backend UX), then a `looksLikeColdStart()` check (timeout OR 502–504 OR ECONNRESET) gates a single retry with 45 s timeout + clear "Backend may be waking up from sleep…" message. Same retry on `cmdStatus`'s health ping. Plus a fire-and-forget warmup `GET /health` at the start of `cmdInstall` so the backend is hot by the time the user clicks the pair URL. — **Windows native labeling.** Static review of all `process.platform === "win32"` branches surfaced three quirks unique to native Windows: (1) `auth.js`'s `openBrowser` uses `start "" "$url"` without `cmd /c`, which fails in PowerShell; (2) the zero-dep multiplexer spawns `claude` which on Windows would need to be `claude.cmd`/`claude.exe`; (3) tmux is not available natively, so `dock` and `attach` would always fall back to multiplexer or fail. Real fix needs a Windows host; until then, README labels native Windows "experimental — use WSL." Mac is unverified but expected to work (the only mac branches — `open` for browser, `darwin` in isHeadless — are correct on inspection). User running real Mac test pass now.
- 2026-05-13: **Stage 10a — Tmux bundle into install + dock toggle stability.** Two tier-1 items folded into one PR. **(1) Ctrl-L second-press crash on macOS.** User report: `Ctrl-L` worked on first press, crashed with `'node dock.js __toggle' returned 1` on second. Root cause: `cli/dock.js`'s `toggle()` invokes `tmuxQuery()` (a thin `execFileSync` wrapper without try/catch) twice per toggle to derive expand-vs-collapse from live pane size; on macOS the second invocation occasionally hits a transient pane state where `display-message` returns non-zero. The exception killed the fresh Node process spawned by tmux's `run-shell -b`, and tmux surfaces non-zero exits as scary popups. Fix: wrap both `tmuxQuery` reads + both `tmuxRun` writes in try/catch with safe defaults (24-row terminal, COLLAPSED_ROWS bottom). Linux/WSL keeps the happy path; Mac stops crashing on the unhappy one. **(2) Bundle tmux install into `waiting-lounge install`.** Per user direction "lets leave the tmux in this case, but can we pack it as a part of lounge installation?": new `ensureTmux()` helper auto-runs `brew install tmux` on macOS-with-brew (no sudo; brew detects existing installs), prints the right `sudo apt/dnf/…` line on Linux/WSL (never auto-sudos), points native Windows users to WSL. Skips silently when tmux is already there. `--no-install-tmux` flag for opt-out. Shared logic extracted to new `cli/lib/tmux.js` (also imported by `cli/attach.js`, replacing its inline `hasTmux`). Reinforces dock as the default flow on Mac since the user no longer has to know about `brew install tmux` separately.
- 2026-05-13: **Stage 10b — Defer auth until pool match.** User direction: "We shouldnt ask for email when opening the software, maybe only when start pairing." Pairing here meaning real-points pool matches; bot games should stay anonymous. Implementation splits cleanly: (a) `cli/lib/auth.js` gains `getStoredToken()` that returns a usable cached/refreshed token or null without ever triggering a pair flow; (b) `cli/play.mjs` initial state moves to `appPhase: "connecting"` and the auth+socket effect uses `getStoredToken()` instead of `getAccessToken()`, falling through to an anonymous socket connect when no token exists; (c) new `runAuthAndJoinPool()` helper triggers the existing browser pair flow when the user picks `[F] find a match` while unauthenticated, then reconnects with the token and queues for pool; (d) backend `start_bot_match_now` no longer requires `me.userId`, and `startBotMatchFor` accepts a synthetic `anon:${socketId}` for the player record — safe because Stage 3b.3 already guarantees bot matches skip `chargeAntes`/`settleGame`. UX: the lobby identity row shows `○ anonymous · handle …` (hollow dot, warning color) until signed in; `[F]` is annotated "(signs you in first)", `[B]` is annotated "(no sign-in needed)". Auth never appears unless the user explicitly picks pool. Ground Rule #11 (frictionless-first) compliance: first-time UX is now zero prompts before they can play. Stage 10c (terminal OTP) will add a `[T]` choice in the auth phase so users never have to leave the terminal.
- 2026-05-13: **Stage 10c — Terminal-side OTP.** User Q2: "Can we avoid the browser step for 'email code receive', and do it in the terminal instead?" Yes — Supabase's REST API exposes `/auth/v1/otp` (request email) and `/auth/v1/verify` (submit 6-digit code) using the public anon key. Backend gains `GET /api/cli/auth/config` that returns `{supabaseUrl, supabaseAnonKey}` from env so the CLI doesn't have to hardcode environment-specific values. New env var on Render: `SUPABASE_ANON_KEY` (anon, public-safe — same one in every browser bundle). CLI helpers in `cli/lib/auth.js`: `fetchSupabaseConfig`, `requestOtp`, `verifyOtp`, `persistTerminalSession` (writes the same token-file shape as browser flow). New `cli/components/AuthPrompt.mjs` owns the state machine: choice → email → sending → code → verifying → done/error. Email validated by regex; code auto-submits at 6 digits. Rate-limit (429) surfaces as "Wait a minute and try again." UX: `[F]` while anonymous opens `[B]rowser / [T]erminal` chooser (or jumps straight to terminal if headless — DISPLAY/WAYLAND_DISPLAY absent, non-WSL non-Mac non-Win, $BROWSER unset). Browser path runs unchanged. Terminal path never leaves the TUI. Final result identical: same token file, same socket reconnect, same `queue_for_pool` emit afterward.
- 2026-05-13: **Stage 10d — Symmetric uninstall + install/uninstall doc.** Before this, `waiting-lounge uninstall --force` removed `~/.waiting-lounge/` but left settings.json entries — users had to hand-edit JSON to clean up. Asymmetric with the auto-merge install (Stage 9). Fix: new `unmergeWaitingLoungeHooks()` is the precise inverse of `mergeWaitingLoungeHooks` — filters out inner hooks whose `command` references `HOOK_PATH`, drops now-empty wrapping blocks, drops now-empty event entries, drops the empty `hooks` container. Timestamped backup written before edit (same path scheme as install). Other tools' hooks + top-level keys (e.g. `theme: "dark"`) preserved. `--keep-settings` opts out for users who want to leave settings.json alone. Tmux is intentionally NOT auto-uninstalled — other tools may depend on it. Plus new `docs/install.md`: one-screen reference covering install + sign-in + open + diagnostics + uninstall + clean-slate reinstall. README links it.
- 2026-05-13: **Stage 10e — Attach Mac-friendly bindings.** User report after testing on Mac: "Cannot use control b up and down to change. Also, control B plus up, is overlapped with the existing MAC ones. Change it." Root cause: macOS Mission Control intercepts `Ctrl + arrow` at the OS level for Space-switching, so tmux never sees the prefix-then-arrow sequence even though the bindings are registered. Fix: `cli/attach.js` now captures the lounge + claude pane IDs at attach time and binds `Ctrl-L` (no prefix needed) in tmux's `root` keytable. Single keystroke, works from either pane, no conflict with macOS. Same pattern as `dock` (which already used this approach successfully). Pane IDs baked into the bind command at registration time so the toggle works regardless of intervening tmux activity (e.g. user creates additional panes). Toggle handler wraps every `tmuxQuery`/`tmuxRun` call in try/catch per the Stage 10a lesson — failure modes degrade to silent no-op instead of scary tmux popups. Trade-off: the binding lives in the tmux server's root keytable, so it persists until `tmux unbind -T root C-l` or `tmux kill-server`. Documented in the post-attach instructions. Acceptable: same trade-off as dock, and users who reach for `attach` are already power-user enough to understand "tmux's runtime bindings reset on server restart."
- 2026-05-14: **Stage 11a — TUI Q-key flow fix.** User report after Mac testing: "when I am in bot or real matching, if I hit q to quit the current process wishing to return to the lobby; I will directly quit the lounge. We should return to the main page." Audit found Q called `cleanExit` (app exit) in every phase except `in_match` (which only opened the forfeit dialog whose Y ALSO did `cleanExit`). Fix: Q is now phase-aware — `in_match` → forfeit confirm; `searching` → cancel queue + lobby; `match_end` → lobby; `lobby` → new `quit` confirm dialog (user picked confirm-then-exit over direct exit); `connecting/pairing/error` → exit. `Esc` from the lobby still exits immediately as a power-user shortcut. Backend needed a new `forfeit_game` socket event: `leave_room` only ever handled chat rooms (touches the `rooms` map, not `games`/`roomGame`), so for a real game-forfeit-that-keeps-the-socket-alive we route through `runner.handleDisconnect(socket.id)` — the same path the disconnect handler uses, so the opponent's grace-timer-win still fires — then clear `me.roomId` so the forfeiter can re-queue without reconnecting. Disconnect+reconnect was rejected: the reconnect-handback logic (sockets.ts ~line 304) would pull an *authenticated* forfeiter straight back into the game they just left. `RETURN_TO_LOBBY` reducer now also clears `confirmDialog` + `toast`. Verified end-to-end by driving the real `play.mjs` in a node-pty PTY: bot match → Q → forfeit dialog renders fully (title + hint, tightened to 2 lines so it doesn't clip under the tall in-match view) → Y → back at lobby, app never exited; lobby Q → quit confirm → N stays / Y exits.
- 2026-05-21: **Stage 12 — Scope reset for the in-terminal app: betting-game + chat only.** User direction after considering Anthropic's Agent View doc: "for the dock, in-chat app we build, we should keep only the game feature and the in-game chat feature… focus only on the betting games, making the betting game features complete." Two consequences. **(a) Stage 11b's board / leaderboard / profile are removed from the TUI lobby** — they remain on the web, but the in-terminal surface now shows only game + chat. The components are kept on disk so a future re-introduction is cheap. **(b) Betting-game lobby gets the full host / browse / join-by-code / preview pipeline** that a real chip-stack player would expect. The four moving parts: HostedRoom registry (one open public room per user, 10-min TTL, 6-char no-ambiguity codes), browse list (polled every 5 s), join-by-code (normalizes whitespace + dashes + lowercase), and a 15-second match-preview before chargeAntes runs on any pairing. Source field on each preview ("pool" / "hosted_room" / "invite") lets the TUI render "matched from the pool" vs "joined via room code" honestly. Public rooms are also eligible for pool matchmaking (the queuer inherits the host's stakes and can decline). Why a preview-before-commit invariant: the player perspective question was "what if I hate the stakes I got matched into?" — the preview answers it without making either side feel ambushed.
- 2026-05-21: **Match-preview invariant.** Every two-human pairing — pool queue match, hosted-room join via code or browse, OR via the random pool consuming a hosted room — opens a 15-second `MatchPreview` between the two sockets BEFORE `chargeAntes` runs. Both sides must accept; either declines (or times out) and both return to the lobby. If the joiner came in via a hosted room, the room re-opens for the next joiner. Antes are charged ONLY on the consume path. Invites (existing `invite_to_game` / `accept_invite`) intentionally skip the preview because the invitee already explicitly opted in. Bot matches skip the preview because they're not human-vs-human. The invariant ties to the user's "I want to see the game before joining" ask and gives the seventh ground-rule "no fake data masquerading as real" a player-facing analogue: no stakes get committed without an explicit live opt-in.
- 2026-05-21: **Room code alphabet — no-ambiguity.** 23 letters + 8 digits, dropping 0/O/1/I/L. 31^6 ≈ 887M combinations is overkill for our in-memory registry; the real win is that a player reading "K7-XQM4" off a screenshot or out loud cannot mistype it. Backend normalizes spaces / dashes / case before lookup so any natural-looking variant a friend types still resolves.
- 2026-05-14: **Stage 11b — Board + leaderboard + profile in the TUI.** User asked to "try adding other functions (as we currently do on the website) to it." Of the 7 web-only features (board, leaderboard, /me, daily, invites, standalone chat, settings) the user picked board + leaderboard + profile as the priority subset. Each ships as a self-contained ink component (`cli/components/Board.mjs`, `Leaderboard.mjs`, `Profile.mjs`) that fetches its own data and owns its own keystrokes — same architecture as `AuthPrompt`. The lobby gains an explore row (`[M]`/`[L]`/`[H]`); the parent `useInput` short-circuits while a child scene is mounted, and `renderFooter` returns null for child-owned phases (which also fixes a pre-existing double-footer on `auth_choice`). New shared helper `cli/lib/api.js` wraps Node 18's global `fetch` + `AbortController` with a 40s timeout for Render cold-starts — no new dependency, and deliberately NOT reusing the `httpRequest` in `cli/lib/auth.js` (that one is callback-shaped and tuned for the auth flow; a fresh promise-based helper is cleaner for read-only GETs). Backend needed zero changes — `GET /api/board`, `/api/leaderboard`, `/api/me`, `/api/me/game-history` all already existed. Board is read-only in the TUI for now (posting needs a text-input flow — deferred). Verified end-to-end by driving the real `play.mjs` in a node-pty PTY: each scene opens, renders live data (leaderboard medals + points, board posts with tags/timestamps), and `Q` returns to the lobby without ever exiting the app.
